# 问题清单

> 记录常驻约定、本轮已落实、下轮需确认的问题，以及后期准备实现的部分功能。本文档每轮重新编号。

> 已确认的规则细节见 `docs/rules.md`（不再附于此文件）。

> 可参考的规则集（玩家自发测试整理，腾讯文档）：https://docs.qq.com/doc/DZkt6Y0JCZFBwaUdK

## 📌 常驻约定

1. 加入含相应效果的卡牌/能力时，其结算流程逐条向你确认（如「给与破甲」那样的细流程）。
2. 简化建议持续有效：已确认流程中我认为可不照搬原版的地方会继续提出，由你决定是否采纳。
3. 每轮对话结束时我会更新本文件，汇总本轮新产生的问题和未关闭的旧问题。
4. 你给出的所有事件结算流程等规则细节会全部保留在 `docs/rules.md`，保留后续重新讨论的可能性。
5. 每轮给出新规则细节时，同步进行代码实现并对现有代码作调整。
6. `questions.md` 中“本轮已落实”仅作为**本轮及近期已确认规则细节的摘要**，用于新一轮规则给出时对照并合并入 `docs/rules.md` 与代码；不长期占用本文档。
7. **进入 Phase 2 服务端实现前，先查看 `server/README.md` 中的架构设计与待确认问题**，确保断线重连、超时结束回合、死循环防护等关键点已讨论并确认。

## 📝 本轮待确认 / 待补充

本轮无新待确认问题。

## ✅ 本轮已落实

近几轮已落实的规则细节已核对并并入 `docs/rules.md` 与代码实现，此处清空。

## 🔮 Phase 3+ 预留机制

1. **鼓舞/压制与战力**：`basic_boost`（正负值）与 `combat_power` 的规则细节——第一个大型卡包一并给出？（机制引入时说明）
2. **使用方式机制**：爆能 burst / 赐能 bless / 起源 origin / 连引 link / 连锁 chain / 戏法 trick 的准确规则；含对局中动态赋予卡牌效果。（机制引入时说明）
3. **幻境机制**：幻境牌 field、幻境队列、耐久 intensity 等规则。（机制引入时说明）
4. **变形机制**：变形事件流程已写入 `docs/rules.md`，具体实现随第一张变形/觉醒牌引入时再确认。（机制引入时说明）
5. **进场顺序动态分配**：当前 `entry_order` 仅在初始化时赋值；Phase 3+ 有式神离场/进场（召唤物、变形解除等）时，需要维护每名玩家的全局进场顺序计数器，为新进场式神分配递增编号。
