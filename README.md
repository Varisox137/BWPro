# BWPro

受《阴阳师：百闻牌》启发的数字化卡牌对战游戏。长期目标为双人联机对战与图形客户端。

当前已实现：核心规则引擎、正式卡牌数据（47 式神 337 卡，含协战牌；经典包完结、不夜之火包，
含 20200520 平衡性数据）、卡组构筑与卡组码、
CLI 热座对战、FastAPI/WebSocket 联机对战（房间制、限时、断线重连）。
测试 740 个，按机制/主题归组为 17 个测试文件。

## 快速开始

```bash
uv sync                              # 安装依赖
uv run python -m client.cli          # 主菜单：热座对战 / 卡组构筑 / 联机对战
uv run python -m server.main         # 联机服务端（默认 127.0.0.1:1037）
uv run python -m client.net          # 联机客户端（创建/加入房间）
uv run python -m db.validate         # 校验卡牌数据库
uv run python -m db.scaffold -h      # 式神/卡牌 yaml 脚手架（生成骨架再补效果块）
uv run pytest -q tests/              # 运行全部测试
```

## 项目结构

- `core/` — 规则层：状态模型、引擎、动作原语、事件、目标解析、对局组装、调试指令
- `db/` — 卡牌数据库（YAML 按 `db/<版本包>_<拼音>/<包内编号>_<式神拼音>/` 分目录，
  式神与其全部卡牌同目录，见 `db/packs.py`）、schema、组卡校验（天梯规则，可参数化）、卡组码
- `client/` — CLI 客户端：`cli.py` 热座 + 卡组构筑；`net.py` 联机（同一 `cmd dict` 协议）；
  `tui.py` TUI 基座（prompt_toolkit 底部输入框 + 常驻状态行，非 TTY 自动回退内置 input）
- `server/` — authoritative 联机服务端（FastAPI + WebSocket，详见 `server/README.md`）
- `diy/` — 自定义卡牌 DSL（Phase 3） · `docs/` — 规则与术语文档 · `tests/` — 测试

## 已实现机制

- 引擎管线：指令校验 → 动作 → 事件 → 触发队列 → 效果块结算；状态纯数据可序列化
- 核心操作：抽牌、伤害、治疗、护甲、召唤、形态结附/消灭、升级、出击、完整战斗流程
- 战斗关键字：连击/先攻/贯通/穿刺/远程/不屈/迅捷/屏障/追猎/直击/帷幕/必杀；
  全量伤害时点批次管线；关键字三类持久性（一次性/持续/永久）；
  作用域战斗伤害免疫与敌方非战斗伤害免疫；有目标的战斗与战斗结束追加攻击
- 觉醒能力替换、攻击后到期强化、倒计时系统、投射/鼓舞、直接消灭、
  随机生成、随机分配伤害（逐点插入结算）
- 效果发起的攻击（不耗火/次数）、法术回响序列（凭空自动使用、出牌来源/方式区分）、
  反击贯通例外、力量覆写修饰、投射贯通、永久力量快照增伤、历史峰值力量差值
- 破甲（负护甲）与吸血、使用手牌前无效化、协战牌（双归属、子选项打出）
- 增强装配管线：卡牌触发器、打出装配快照、enhance 数值叠加、卡牌光环、
  战斗绑定一次性触发、计数次档随机强化（按实例去重）、牌手级持久监听
  （本局/本回合作用域）与随机四选一监听
- 响应插入使用、延迟触发、伤害上限、激怒与战斗区锁定、
  进场时形态效果与动态数值（护甲/力量求值）
- 手牌触发式光环（其他己方式神攻击后本回合瞬发）、回合结束响应排序
  （当前回合方延时效果先于对方手牌响应）、生命下限钳制与本回合受伤记账、
  无当前战斗的响应战斗牌完整流程、指定准备区目标的可选战斗
- 弹回与庇佑关键字、伤害转化为破甲与穿刺仅护甲变体 pierce_armor（伪关键字通道）、
  连续型动态身材光环（读取时求值缓存）、回合开始抽牌替换（检视挑选）、
  战中调度（choose 作答）、
  手牌上限爆牌统一路径、空库燃烧替换判负、敌方破甲保留、牌库检索直接使用形态牌
- 运势判定：1d6 六时机管线（必 6 骰子修饰、判定时重投、判定后延时触发、
  觉醒翻倍）、并行事件同步推进与双方各自判定、骰子历史/成功计数记账、
  [条件] 使用前提；眩晕（普通/持续条目、出牌/出击/响应门控、回合结束解除）；
  变形（快照还原、所属式神、气绝前2 解除、变形物定义）
- 破甲等量转移（源清零、可敌方全体）与式神级破甲保留（形态结附期间）；
  眩晕事件（施加点即时时机）与眩晕条件体系（目标过滤/选择目标/交战对方/事件字段）；
  卡牌光环伤害加成通道（tags 谓词、本局作用域）与按 id 永久力量光环（结附牌手、跨召唤）；
  战斗作用域关键字授予、不能攻击的召唤物、先天额外鬼火伪关键字；
  永久变形（不还原）与变形物战斗牌白名单、手牌/牌库生成替换钩子、
  凭空自动使用（目标继承）与定向攻击（战斗目标取选择目标）、
  气绝时可用战斗牌（结算后未气绝则正常发起战斗）、按 tags 的出牌计数记账
- 「已展示」手牌机制：实例级展示标志（本局保持、随实例、调度传递）、
  随机/指定式神/全体/事件四档展示（协战归属统一口径）、入手统一事件钩子
  （抽牌/生成/检索/检视/调度换入，起始手牌与爆牌除外）、敌方已展示手牌强制调度、
  按展示计数的动态数值与条件关键字、已展示手牌额外耗火（形态结附期间）、
  本回合伤害记账过滤；联机脱敏对已展示卡放行
- 能力进场统一事件钩子（对局开始/升级/复活/觉醒替换/变形与还原）、
  能力型卡牌光环（挂能力持有者、任意所属式神通配、随能力离场清理）、
  倒计时复原（不触发归零）、手牌卡牌原位变换、条件破甲转化（仅本有破甲者）
- 能量系统与[充能]（式神级能量上限 10、回合开始充能、获得能量事件）、
  爆能（使用方式支付能量追加效果、爆能X 消耗全部能量快照、多档累计、
  方式临时授予关键字）、能量生命代偿与每回合一次免单、能量出击；
  [移动]（战斗区↔准备区 toggle、强制拉入过战斗区锁定校验、半回合移动计数）；
  鼓舞扩展旗标（战斗牌攻击消耗出击加成、加成不消耗、鼓舞数值加算）；
  取消攻击/攻击替换/交战改向、指定牌复制使用与分身复制法术、持续眩晕
  （按事件解除、防自解除）、召唤继承身材与能量比例、恢复至满；
  破甲来源免疫（持破甲敌方式神的伤害）、事件数值修饰（封顶/减半向下取整）、
  按[暴击]时机的破甲双倍（本战斗牌发起的战斗、反击/嵌套不翻倍）、
  连击第一段击杀交互（贯通改打牌手/否则终止）
- 治疗管线扩展：治疗时/治疗后双时机（零量治疗只触发前者）、过量治疗记账
  （overheal）与转化通道、治疗反转、牌手级全伤害免疫；鬼火储存（回合开始不清零、
  累加封顶）与鬼火变化事件（付费点即时、可挂"鬼火变为 N"响应）；
  结算中交互选择（pending_choice + choose 指令：牌库顶检视挑选，
  重连续答、回合超时随机作答）；直接升级/生命设置/复活与形态实例重新结附等效果原语
- 对局流程：调度阶段、先后手经济、气绝/复活、长对局平局、调试指令系统
- 卡组：交互式构筑、天梯规则校验（4 式神 / ≤2 派系 / 各恰好 8 张 / 同名 ≤2）、
  卡组码导出导入、本地卡组文件（~/.bwp.decks.json）槽位管理（编辑/重命名/删除）与战前选卡；
  构筑环境（平衡性版本日期）可按卡组指定，仅列出该日期已发布的式神/卡牌并按当时数据展示；
  环境输入支持别名（经典/不夜之火等，见 `db/envs.py`）与 8/6 位日期，卡牌两行式打印（数值首行 + 效果次行）
- 联机：房间创建（可自建房码）/按房码加入、准备流程（双方未准备不计时 → 一方准备
  对另一方 15s 自动准备 → 双方准备 3s 开始倒计时，可取消准备/离开）、随机先手、
  对局模式（标准 = 固定最新环境；自由 = 建房或双方未准备时房主指定环境，
  数据按不晚于该日期的最晚平衡性版本解析）、
  token 断线重连、调度 30s/回合 120s 计时、debug 对局与服务端控制台、
  状态按视角脱敏等基础安全（详见 `server/README.md`）
- CLI 显示：座次配色、修饰状态、关键字中文化、手牌数值段（含实时增强求值——
  持久计数未装配也按当前值显示）；
  结算明细（等级/力量/战力/生命/护甲/破甲变化与战斗、伤害、治疗、运势、
  气绝、倒计时、回合阶段事件）与叙事日志按合并时间线排队播放（0.3s/条、块前后空行、
  嵌套层级缩进，热坐与联机一致），播放期间可正常输入操作（client/settle.py
  打印队列）；对手回合/等待期不显示输入提示符，终局按视角播报结果；
  TTY 下底部输入框 + 常驻状态行（双方牌手信息 + 回合，联机另显示倒计时）

## 主要协议

玩家指令为 `cmd dict`，热座 CLI 与联机客户端共用：

```json
{ "op": "play_card", "uid": 1, "target": { "player": 1, "shikigami": 0 } }
{ "op": "assault", "index": 0 }
{ "op": "upgrade", "index": 2 }
{ "op": "choose", "uid": 42 }
{ "op": "end_turn" }
```

- 对局阶段：`mulligan` → `upgrade`（每回合开始）→ `battle`（主要阶段）
- `choose` 仅在结算中交互选择（pending_choice，如牌库顶检视）挂起时可用，uid 为选项卡牌实例
- CLI 序号显示与输入为 1-based，协议层保持 0-based index

## Roadmap

1. **Phase 1 ✅** 核心规则引擎 + 数据模型 + 正式数据 + CLI 热座 + 卡组构筑
2. **Phase 2 ✅（初版）** 联机服务端与联机客户端（回放/自动匹配待后续）
3. **Phase 3** 自定义卡牌 DSL 编译器（`diy/`）
4. **Phase 4** 图形客户端
5. **Phase 5** 进阶机制：持续效果与光环、出击增减益、赐能/起源等

## 设计文档

- `docs/rules.md` — 已确认规则细节全集 · `docs/terminology.md` — 术语与代码命名对照
- `docs/card-status.md` — 卡牌实现状态 · `docs/enhance-design.md` — "增强"机制设计结论
- `card_data_raw.md` — 原版卡牌描述的唯一事实来源：式神/卡牌新增与描述更新先在此登记，
  再按机制落地批次录入 `db/`

## 声明

- 本项目为**非盈利**的同人/学习作品，与网易及《阴阳师：百闻牌》官方无关；规则与数值实现**可能与原版存在差异**（以 `docs/rules.md` 为准）。
- 如有侵权内容，请联系删除，即行移除。
- 欢迎合作开发者与 DIY 卡牌内容提供者参与（自定义卡牌见 `diy/README.md`）。
