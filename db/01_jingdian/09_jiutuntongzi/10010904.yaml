# 脚手架生成（db.scaffold），待补效果块与卡面文本
id: 10010904
version: 20260730
name: 狂啸
shikigami: 100109  # 派系：红莲（继承自式神数据）
card_type: spell
rarity: R
level: 2
cost: 1
keywords: [trigger]
effects:
  # 本回合酒吞童子生命不会降到 1 以下：ext["min_health_turn"] 置位
  # （半回合作用域，回合开始清除；扣减生命处钳制）
  steps:
    - {op: bump_ext, key: min_health_turn,
       target: {kind: all, pool: friendly_shikigami, shikigami: 100109}}
response:
  # [响应]：{当酒吞童子将受到伤害时，自动使用}——挂伤害开始时批次（参照
  # 穿刺"造成伤害前"）；主动/响应效果同构，响应走覆盖块挂响应时机
  when: on_damage_start
  condition: {victim_shikigami: 100109, victim_side: friendly}
  steps:
    - {op: bump_ext, key: min_health_turn,
       target: {kind: all, pool: friendly_shikigami, shikigami: 100109}}
text: "本回合酒吞童子生命不会降到1以下。[响应]：{当酒吞童子将受到伤害时，自动使用。}"
