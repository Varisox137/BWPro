# 脚手架生成（db.scaffold），待补效果块与卡面文本
id: 10012901
version: 20260802
name: 金运大吉
shikigami: 100129  # 派系：苍叶（继承自式神数据）
card_type: form
rarity: R
level: 1
cost: 1
form_power: 3
form_health: 6
effects:
  steps:
    # 进场：双方牌手各做运势4判定（judge=both，当前回合玩家先）；
    # 判定者各自抽1——then 步以判定者为效果归属结算（需引擎侧确认该语义）
    - op: luck_roll
      x: 4
      judge: both
      then:
        - {op: draw, count: 1}
abilities:
  # 己方回合开始时：同上
  - when: on_turn_start
    condition: {player: self}
    steps:
      - op: luck_roll
        x: 4
        judge: both
        then:
          - {op: draw, count: 1}
text: "进场和己方回合开始时，双方牌手[运势4]：{抽一张牌。}"
